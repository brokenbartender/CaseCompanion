# Proof Packet Golden Master

Place the known-good proof packet PDF here and record its SHA-256 hash.

Recommended command:
- `npm run verify:packet -- <packet.zip> --record`

This will copy the PDF from the packet into this directory and write `proof_packet.pdf.sha256`.
